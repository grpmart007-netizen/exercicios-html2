const nomes = ["Jotta", "Gisley", "Giovana"];

const nomeEncontrado = nomes.find(nome => nome === "Matheus");
// findIndex encontra a posição da palavra via index
// includes procura as primeiras letras e devolve o nome completo

console.log(nomeEncontrado);