// const valores = [10, 20, 30,40];

// valores.forEach((valor, i, array) => {
//   array[i] = valor * 2; // Modifica o array original
// });

// console.log(valores); // [10, 20, 30, 40];\

const numeros = [10, 20, 30, 40];

numeros.forEach(numeros(100));
console.log(100);